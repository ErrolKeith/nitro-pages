import { makeAnchor, AnchorConfig, AnchorProps } from "./a";
import { makeBody, BodyConfig, BodyProps } from "./body";
import { makeDiv, DivConfig } from "./div";
import { makeFooter, FooterConfig } from "./footer";
import { makeHead, HeadConfig, HeadProps } from "./head";
import { makeHeader, HeaderConfig } from "./header";
import { makeHeading, HeadingLevel, HeadingProps } from "./heading";
import { makeHtml, HtmlConfig, HtmlProps } from "./html";
import { makeMain, MainConfig } from "./main";
import { makeMeta, MetaProps } from "./meta";
import { makeParagraph, ParagraphConfig } from "./p";
import { makeSection, SectionConfig } from "./section";
import { makeTitle, TitleProps } from "./title";
import { Tag } from "./schema";

export {
  makeAnchor,
  makeBody,
  makeDiv,
  makeFooter,
  makeHead,
  makeHeader,
  makeHeading,
  makeHtml,
  makeMain,
  makeMeta,
  makeParagraph,
  makeSection,
  makeTitle,
  AnchorConfig,
  AnchorProps,
  BodyConfig,
  BodyProps,
  DivConfig,
  FooterConfig,
  HeadConfig,
  HeadProps,
  HeaderConfig,
  HeadingLevel,
  HeadingProps,
  HtmlConfig,
  HtmlProps,
  MainConfig,
  MetaProps,
  ParagraphConfig,
  SectionConfig,
  Tag,
  TitleProps,
};
